import React from "react"
import CountdownTimer from "../CountdownTimer/CountdownTimer"

const BreakTime = ({ defaultTime }) => {
  return <CountdownTimer defaultTime={defaultTime} />
}

export default BreakTime
